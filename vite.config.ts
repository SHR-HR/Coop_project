// Импорт функции defineConfig из Vite для типобезопасной конфигурации
import { defineConfig } from 'vite'
// Импорт плагина React для Vite (обеспечивает HMR, JSX трансформацию)
import react from '@vitejs/plugin-react'

// Экспорт конфигурации Vite по умолчанию
// https://vite.dev/config/ - ссылка на документацию Vite
export default defineConfig({
  // Массив плагинов Vite
  plugins: [
    // Плагин React с настройками по умолчанию
    react()
  ],
})




/* ===== ПОЯСНЕНИЯ К КОММЕНТАРИЯМ ДЛЯ vite.config.ts ===== */

/*
1. Назначение файла:
   - Конфигурация сборщика Vite для проекта
   - Определяет как Vite будет собирать и обслуживать приложение

2. Плагин @vitejs/plugin-react:
   - Обеспечивает поддержку React в Vite
   - Включает Fast Refresh (HMR) для React компонентов
   - Автоматически настраивает JSX трансформацию

3. Минималистичная конфигурация:
   - Vite работает с настройками по умолчанию для большинства случаев
   - Дополнительные настройки могут быть добавлены по мере необходимости

4. Возможные расширения конфигурации:
   - Настройка сервера разработки (port, proxy)
   - Конфигурация сборки (output paths, chunk splitting)
   - Оптимизации для production
   - Интеграция с другими инструментами
*/